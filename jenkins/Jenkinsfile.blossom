podTemplate (cloud:'sc-ipp-blossom-prod') {
    node(POD_LABEL) {
        stage('Code checkout method1') {
            checkout scm
        }
        stage ('Build') {
           sh "ls -l"
        }
    }
}
